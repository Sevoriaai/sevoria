# Sevoria

A sharp, honest AI assistant. Static site (HTML/CSS/JS), deploys on Vercel.

## Security
No secrets live in this repo. The Supabase **anon** key is public by design (protected by Row Level Security). The **service_role** key and any model keys stay in server-side env vars only — never here.
